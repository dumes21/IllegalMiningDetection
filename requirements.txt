# Dataset download
huggingface_hub==1.28.0

# Raster IO and analysis
rasterio==1.5.1
numpy==2.5.2
pandas==3.0.5
matplotlib==3.11.1

# Notebook kernel
ipykernel==7.3.0

# TerraMind foundation model (pulls torch, lightning, timm, torchgeo, ...)
# Install torch first with the CUDA build matching your GPU if training locally.
terratorch>=1.2.13
setuptools<81
torchgeo==0.9.0 #0.9.0 has support for terratorch 


#GEE
earthengine-api 
geemap